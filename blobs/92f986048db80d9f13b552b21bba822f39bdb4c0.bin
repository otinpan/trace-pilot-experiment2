# Record 1: file:///home/hase/trace-pilot-experiment2/src/navigator.cpp
```diff
===================================================================
--- navigator.cpp (before)	snapshot
+++ navigator.cpp (after)	idle
@@ -62,9 +62,22 @@
       dist[next->i][next->j]=d+1;
       pre[next->i][next->j]=ActionRecord{ActionType::SLIDE,now};
     }
   }
-  return {};
+
+  if(dist[to.i][to.j]==-1){
+    return {};
+  }
+
+  vector<ActionRecord> path;
+  Pos cur=to;
+  while(cur!=from){
+    const ActionRecord record=pre[cur.i][cur.j];
+    path.push_back(ActionRecord{record.type,cur});
+    cur=record.pos;
+  }
+  reverse(path.begin(),path.end());
+  return path;
 }
 
 // @trace-pilot d0b54a024abf0bd00d8ee52220a603d60c21a363
 void Navigator::change_block(const Pos& pos){

```

まず該当する `navigator` 実装と `ActionRecord` の形を確認します。既存の型と経路復元の書き方に合わせて、差分として提案できる形にします。

`bfs` はすでに `pre` に「そのマスへ来る直前の位置と操作種別」を入れる構造になっていそうです。復元処理だけ足せば済むか、型の意味がズレていないかをファイルで確認します。

経路復元では `pre` 内の `ActionRecord::pos` を「復元用の親」として使い、返す配列では `pos` を「その操作後に到達する位置」として詰め直すのが扱いやすいです。`from == to` は操作不要なので空配列、到達不能も空配列にします。

`bfs` の末尾に復元処理を入れました。次に、このプロジェクトのビルド方法でコンパイルが通るか確認します。

差分表示が空だったので、ワークツリー側の状態をもう一度確認します。パッチ自体は成功しているため、ファイル内容と Git の追跡状態を見ます。

`src/navigator.cpp` は Git 上では未追跡ファイルなので `git diff -- src/navigator.cpp` には出ていませんでした。内容は更新済みです。ビルドして型エラーがないか確認します。

`bfs` の末尾に経路復元を入れました。変更箇所は [src/navigator.cpp](/home/hase/trace-pilot-experiment2/src/navigator.cpp:66) です。

考え方はこうです。

```cpp
if(dist[to.i][to.j]==-1){
  return {};
}

vector<ActionRecord> path;
Pos cur=to;
while(cur!=from){
  const ActionRecord record=pre[cur.i][cur.j];
  path.push_back(ActionRecord{record.type,cur});
  cur=record.pos;
}
reverse(path.begin(),path.end());
return path;
```

`pre[next]` には探索中、「`next` へ来るために使った操作」と「直前の位置 `now`」を入れています。復元時は `to` から `from` まで親をたどり、返す `ActionRecord` では `pos` を「その操作後の到達位置」として詰め直しています。

つまり返り値は、

```cpp
{ ActionType::MOVE or SLIDE, 到達先Pos }
```

の列になります。

`from == to` の場合は操作なしなので空配列、到達不能の場合も空配列です。`cmake --build build` は通っています。